<h1>Create Your Costume</h1>

<% if @costume.errors.any? %>
  <div class="error_explanation">
    <h3><%= pluralize(@costume.errors.count, "error") %> prohibited this user from being saved: </h3>
      <% @costume.errors.full_messages.each do |error| %>
        <p>*<%= error %>*</p>
      <% end %>
  </div>
<% end %>

<%= render 'form' %>
